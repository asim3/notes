..title..
Docker Engine - Community

..link.. devops/docker.txt
..



..code..
docker-compose version


..link.. https://docs.docker.com/compose/install/#install-using-pip#install-compose-on-linux-systems
install


..code..
sudo curl -L "https://github.com/docker/compose/releases/download/1.25.4/docker-compose-$(uname -s)-$(uname -m)" -o /usr/local/bin/docker-compose

sudo chmod +x /usr/local/bin/docker-compose


..code..
mkdir -p ~/test-compose/ && nano ~/test-compose/docker-compose.yml
version: '3'

services:
  my-test-service:
    build: ~/test/
    volumes:
      - ~/test/src/:/var/www/html
    ports:
      - 5000:80

  website:
    image: php:apache
    volumes:
      - ../test/src:/var/www/html/
    ports:
      - 5001:80


..code..
cd ~/test-compose/ && sudo docker-compose config
cd ~/test-compose/ && sudo docker-compose up -d
cd ~/test-compose/ && sudo docker-compose down













..code..
mkdir ~/myproject
nano ~/myproject/docker-compose.yml


..text..
docker-compose.yml
..code..
version: '3'

services:
  web:
    build: .
    command: python my_project/my_project/manage.py runserver 0.0.0.0:8000
    volumes:
      - .:/my_file
    ports:
      - "8000:8000"
    depends_on:
      - db
  db:
    image: postgres


..sub_title..
Check the validity of docker-compose.yml
..code..
docker-compose config
 

..code..
docker-compose run web python my_project/my_project/manage.py migrate
docker-compose build
docker-compose up


..code..
sudo docker-compose up -d
sudo docker ps
sudo docker-compose down


..text..
scale services
..code..
sudo docker-compose up -d --scale database=4
sudo docker-compose up -d --scale web=3
